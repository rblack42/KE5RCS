Gateway DNS Setup
#################

..  include::   /references.inc

The gateway will run a DNS system that manages all |DS| IP address data. The
configuration is set up using the following script:

:ds_bind_install:

..  literalinclude::   files/ds_bind-install
    :linenos:

    
